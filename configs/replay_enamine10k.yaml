campaign_id: replay_enamine10k
seed: 0
executor: replay

data:
  library_csv: data/Enamine10k.csv.gz
  fps_h5: data/Enamine10k.h5
  moo_csv: data/Enamine10k_moo.csv.gz

init_frac: 0.01
batch_size: 100
n_rounds: 5
acquisition: greedy
beta: 1.0
checkpoint_dir: runs/replay_enamine10k

surrogate:
  n_estimators: 100

objectives:
  - name: activity
    kind: expensive
static_properties:
  - name: qed
    kind: static
  - name: sa_ease
    kind: static

async:
  max_active_jobs: 2

benchmark: true
