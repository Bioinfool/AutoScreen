campaign_id: vina_demo
seed: 0
executor: vina

data:
  library_csv: data/Enamine10k.csv.gz
  fps_h5: data/Enamine10k.h5
  moo_csv: data/Enamine10k_moo.csv.gz

init_frac: 0.002
batch_size: 8
n_rounds: 2
acquisition: greedy
beta: 1.0
checkpoint_dir: runs/vina_demo

objectives:
  - name: activity
    kind: expensive
static_properties:
  - name: qed
    kind: static
  - name: sa_ease
    kind: static

vina:
  receptor: null
  box_center: [0.0, 0.0, 0.0]
  box_size: [20.0, 20.0, 20.0]
  exhaustiveness: 4
  num_modes: 1
  cpu: 4
  vina_bin: vina
  work_dir: runs/vina_work
  max_workers: 2
  per_ligand_timeout_s: 120
  max_retries: 1

async:
  max_active_jobs: 2

surrogate:
  n_estimators: 50
